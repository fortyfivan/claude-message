Produce a finished deliverable from a content file.

Identify the asset type from the content file's frontmatter schema field or from the arguments. Load the production type guide, brand tokens, and asset template. Parse content against the schema. Produce the file.

For campaign-wide production, use --campaign [name].

/agents producer $ARGUMENTS
