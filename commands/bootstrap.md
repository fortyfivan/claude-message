Build a complete messaging system from scratch.

Read and follow the instructions in `.claude/skills/messaging/bootstrap/SKILL.md`. Execute every step in order — the skill handles workspace setup, context gathering, and all six phases.

Pass any user-provided arguments as additional context for the bootstrap session.
