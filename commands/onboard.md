Scaffold the messaging workspace — directories, templates, seed files, and project context.

Safe to run on fresh or existing projects. Creates missing structure without overwriting. Surfaces conflicts for resolution when dropped into an existing workspace.

/agents onboard $ARGUMENTS
