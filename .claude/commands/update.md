Update a living artifact — detect drift, propose changes, get approval, version the result.

Artifact: $1 (artifact slug, e.g., first-call-deck)

If no slug provided, lists all artifacts with their drift status.

Examples:
  /update first-call-deck
  /update product-roadmap
  /update

Read and follow the instructions in `.claude/skills/workflows/update/SKILL.md`. Pass all arguments.
