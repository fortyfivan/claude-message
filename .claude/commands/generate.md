Generate a content asset using a messaging skill.

Parse the task for persona, product, competitor, segment, motion, altitude. Resolve each parameter to the messaging house. Load the skill from `.claude/skills/tasks/`. Present a brief for approval before generating.

/agents writer $ARGUMENTS
