Define the company's asset types and variants from its go-to-market motions — the asset layer of the messaging house. Run after `/bootstrap`. Interviews on the campaigns, launches, plays, and events the team runs, calibrates altitude and format from existing examples, then generates every asset envelope + variant in parallel and reconciles the MESSAGE.md `## Assets` catalog.

Read and follow the instructions in `.claude/skills/system/tune/SKILL.md`.
