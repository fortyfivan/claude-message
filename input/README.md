# Input

Place existing materials here for the bootstrap agent to read. Any format works — pitch decks, one-pagers, website copy, brand guides, competitive analyses, call transcripts, research reports, etc.

The bootstrap agent reads this directory first before searching the web or asking questions. Files don't need to match the messaging system format — the agent extracts what it can and maps it to the system architecture.
